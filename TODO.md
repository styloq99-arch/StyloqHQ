# Frontend Run TODO

- [x] Step 1: Install dependencies in frontend/ (completed: `npm install --prefix frontend` and `powershell -ExecutionPolicy Bypass -Command "cd frontend; npm install"`; dependencies up to date, node_modules created)
- [x] Step 2: Start Vite dev server (completed: `powershell -ExecutionPolicy Bypass -Command "cd frontend; npm run dev"`; running on http://localhost:5173/)
